// import React from "react";
// import useRoomStore from "../lib/store";


// type TextProps = {
//   id: string;
//   type: string;
//   price: number;
//   details: string;
// };

// const RoomTextComponent: React.FC<TextProps> = ({ id, type, price, details }) => {
// //   const addRoom = useRoomStore((state) => state.addRoom);

// //   const handleAddRoom = () => {
// //     addRoom({ id, type, price, details, image: "" }, 2, 1); // Example guests and nights
// //   };

//   return (
//     <div className="flex flex-wrap max-w-[360px]">
//       <h3 className="text-lg font-bold">{type}</h3>
//       <p className="text-sm text-gray-600">{details}</p>
//       <p className="text-lg font-semibold text-red-500">${price} per night</p>
      
//     </div>
//   );
// };

// export default RoomTextComponent;